# Как читать эту папку

- `dashboard.html` — открывается сам в начале сборки; можно и двойным кликом. Этапы, прогресс, время, что осталось. Обновляется сам, пока сборка идёт. `index.html` рядом — тот же файл под другим именем.
- `<дата>-<проект>/` — папка одной сборки. Суффикс `--wip` значит «ещё делается». Внутри находятся исходный бриф, манифест, спецификация и таски.

Если сборка прервалась — скажи агенту «продолжи автопилот», он поднимет состояние отсюда.

## Прогоны

| Начат | Папка | Статус | Итог |
|---|---|---|---|
| 2026-09-10 | `2026-09-10-picloms-scroll-archive` | сдан | Scroll-driven витрина picloms с локальными фото, адаптивной галереей и финальным outro |
